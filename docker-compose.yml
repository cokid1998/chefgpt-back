services:
  # PostgreSQL 데이터베이스
  postgres:
    image: postgres:15-alpine
    container_name: chefgpt-postgres-dev
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: chefgpt_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - chefgpt-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  # NestJS 애플리케이션
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: chefgpt-backend-dev
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: postgresql://postgres:postgres@postgres:5432/chefgpt_db
    env_file:
      - ./.env
    volumes:
      # 개발 중 실시간 리로딩을 위해 소스 코드 마운트
      - ./src:/app/src
      - ./prisma:/app/prisma
    command: >
      sh -c "
        echo 'prisma 스키마 세팅...⚙️' && 
        npx prisma migrate dev && 
        echo 'seed 데이터 생성 시작...🌱' &&
        npx prisma db seed &&
        echo '서버시작...🚀' && 
        npm run dev
      "
    networks:
      - chefgpt-network

volumes:
  postgres_data:

networks:
  chefgpt-network:
    driver: bridge
